# Front-end load-balancer service for vault instances
apiVersion: v1
kind: Service
metadata:
  name: vault-svc
  labels:
    app: vault
spec:
  ports:
  - port: 9000
    targetPort: 9000
    protocol: TCP
    name: vaultport
  selector:
    app: vault  
